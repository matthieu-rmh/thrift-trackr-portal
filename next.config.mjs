/** @type {import('next').NextConfig} */

const nextConfig = {
    /* 
        When set to true, the parameter below 
        allow strict mode which makes the callback inside 
        the useEffect hooks to get called twice 
    */
    reactStrictMode: true
};

export default nextConfig;
