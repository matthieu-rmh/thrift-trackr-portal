import { getData } from "@app/lib/data";

export default async function Page(){
    // let data = await getData();
    // console.log(data);
    return(
        <div>
            <p>Item page</p>
        </div>
    );
}